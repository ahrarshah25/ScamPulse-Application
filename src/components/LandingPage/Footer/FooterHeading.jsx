import React from "react";

const FooterHeading = ({ title }) => {
  return (
    <h4 className="text-white font-semibold tracking-wide mb-8">
      {title}
    </h4>
  );
};

export default FooterHeading;
